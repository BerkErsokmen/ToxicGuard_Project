version: '3.8'
services:
  toxicguard:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../models:/app/models
      - ../reports:/app/reports
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped
